name: Deploy

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-images:
    name: Build Docker Images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push web image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.web
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/web:latest
            ghcr.io/${{ github.repository }}/web:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push agent-runtime image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.runtime
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/agent-runtime:latest
            ghcr.io/${{ github.repository }}/agent-runtime:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build-images]
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # -----------------------------------------------
      # Placeholder: Add your deployment steps here
      # Options include:
      #   - SSH into server and docker compose pull/up
      #   - Deploy to Railway, Fly.io, or Render
      #   - Update Kubernetes manifests
      #   - Trigger ArgoCD sync
      # -----------------------------------------------
      - name: Deploy (placeholder)
        run: |
          echo "Deploy step placeholder"
          echo "Image tags:"
          echo "  web: ghcr.io/${{ github.repository }}/web:${{ github.sha }}"
          echo "  agent-runtime: ghcr.io/${{ github.repository }}/agent-runtime:${{ github.sha }}"

      - name: Run database migrations
        run: |
          echo "Migration step placeholder"
          echo "Run: npx prisma migrate deploy"

      - name: Verify deployment
        run: |
          echo "Health check placeholder"
          echo "Verify: curl -f https://your-domain.com/api/health"
